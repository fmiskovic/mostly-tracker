# Project Time Tracking example

### Build project
`./mvnw clean install`

### Run project
`docker-compose up --build`

On the startup, couple of projects and entries will be insert into the db.

### API documentation:

http://localhost:8080/swagger-ui.html
